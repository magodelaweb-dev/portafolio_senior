<% content_for :title, "Nuevo estudio de caso" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-3xl font-bold tracking-tight">Nuevo estudio de caso</h1>
  <p class="mt-2 text-sm text-slate-500">Los campos S/T/A/R aceptan <strong>Markdown</strong> (GFM).</p>

  <%= render "form", project: @project %>

  <%= link_to root_path, class: "mt-4 inline-flex items-center gap-1 text-sm font-medium text-slate-500 transition hover:text-teal-600" do %>
    <span aria-hidden="true">&larr;</span> Volver a proyectos
  <% end %>
</div>
