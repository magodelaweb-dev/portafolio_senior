<% content_for :title, "Editando estudio de caso" %>

<div class="mx-auto max-w-2xl">
  <h1 class="text-3xl font-bold tracking-tight">Editando estudio de caso</h1>
  <p class="mt-2 text-sm text-slate-500">Los campos S/T/A/R aceptan <strong>Markdown</strong> (GFM).</p>

  <%= render "form", project: @project %>

  <div class="mt-4 flex gap-4 text-sm font-medium text-slate-500">
    <%= link_to "Ver estudio de caso", @project, class: "transition hover:text-teal-600" %>
    <%= link_to "Volver a proyectos", root_path, class: "transition hover:text-teal-600" %>
  </div>
</div>
